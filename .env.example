# No production domain is bound yet. This is the current canonical candidate,
# not evidence that DNS, HTTPS, or hosting has been configured.
SITE_URL=https://heresonare.com

# Do not add database credentials until a provider and schema are approved.
# Keep all real .env and .env.*.local files outside version control.
